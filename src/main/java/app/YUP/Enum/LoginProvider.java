package app.YUP.Enum;

/**
 * Enum representing different login providers.
 * This enum is used to identify the source of the login request.
 */
public enum LoginProvider {
    /**
     * Represents the Google login provider.
     * When a user chooses to log in with Google, this value will be used to identify the login source.
     */
    GOOGLE
}
